package collections;

public class CollectionDocs {
	/*
	 * use to store and processing dynamic multiple data/objects.
	 * 1.List : dynamic size, duplicate allowed, insertion order preserved.
	 * a. ArrayList : slow processing, better to use for data store
	 * b. LinkedList : fast processing, better to use for data manipulation
	 * 2.Set : collection of unique data
	 * a. HasSet : unique data only, no order maintained 
	 * b. TreeSet : unique + sorted order
	 * c. LinkedHasSet : unique + insertion order preserved 
	 * 3.Map : Data manage in key, value pairs, duplicate key not allowed, duplicate values 
	 * a. HasMap : <k,v> pair 
	 * b. TreeMap : <k ,v> + sorted order
	 * LinkedHasMap : <k ,v> + insertion order preserved
	 * */
	
	/*
	 *  //create 3 map related questions:
	 *  Q1. Create map of :
	 *  	a. Student name and roll number
	 *      b. Employee name and salary
	 *      c. Product name and price
	 *      
	 *   Q2 . Create a map of country name and its capital city. Print the map in the following format:
	 *       a. Country: <country_name>, Capital: <capital_city> 
	 *       
	 *    Q3. Create a map of book title and its author. Print the map in the following format:
	 *       a. Book: <book_title>, Author: <author_name>
	 *       b. Find the author of a specific book and print the book title and author name.
	 *       c. Calculate the total number of books in the map and print the result.
	 *       
	 *    Q4. Create a map of city name and its population. Print the map in the following format:
	 *         a. City: <city_name>, Population: <population>
	 *         b. Calculate the total population of all cities in the map.
	 *         c. Find the city with the highest population in the map and print its name and population.
	 *         d. Find the city with the lowest population in the map and print its name and population.   
	 *   		  
	 */

}
